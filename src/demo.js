import { scenarioFixtures } from "./demo-model.js";
const select=document.querySelector("#scenario"),scenarioExplore=document.querySelector("#scenario-explore"),scenarioLabel=document.querySelector("#scenario-label"),run=document.querySelector("#run-test"),runAgain=document.querySelector("#run-again"),report=document.querySelector("#view-report"),steps=document.querySelector("#demo-steps"),live=document.querySelector("#demo-live"),result=document.querySelector("#demo-result"),details=document.querySelector("#demo-details"),explanation=document.querySelector("#demo-explanation"),next=document.querySelector("#demo-next"),summary=document.querySelector("#demo-summary"),switchValue=document.querySelector("#demo-switch"),portValue=document.querySelector("#demo-port"),vlanValue=document.querySelector("#demo-vlan"),observationStatus=document.querySelector("#observation-status"),observationDetail=document.querySelector("#observation-detail"),discoveryFact=document.querySelector("#discovery-fact"),pathNodes=["jack","link","dhcp","gateway","dns","internet","discovery","handoff"];let timers=[];
function clean(value){return String(value).replaceAll("Â·","·").replaceAll("swroc26d1a","switch-01.example");}
function detailValue(fixture,key){return fixture.details.find(([label])=>label===key)?.[1]||"";}
function pathIndex(name){return name==="Ethernet Link"?1:name==="DHCP"?2:name==="Gateway"?3:name==="DNS"?4:name==="Internet"?5:name==="LLDP/CDP"?6:name==="Switch / Port"?6:7;}
function resetPath(){pathNodes.forEach((name,index)=>{const node=document.querySelector(`[data-path="${name}"]`);if(node){node.className="";node.dataset.path=name;node.querySelector("i").className="";node.classList.add(index===0?"is-ready":"is-not-tested");}});}
function setPath(stepIndex,step,fixture){const map=["jack","link","dhcp","gateway","dns","internet","discovery","handoff"],stepMap=[0,0,1,2,3,4,5,7];map.forEach((name,index)=>{const node=document.querySelector(`[data-path="${name}"]`);if(!node)return;node.classList.remove("is-ready","is-pass","is-fail","is-observed","is-not-tested","is-active");const source=fixture.steps[stepMap[index]];let tone="is-not-tested";if(index===0)tone=stepIndex>=0?"is-pass":"is-ready";else if(index===stepIndex)tone=step[3]==="fail"?"is-fail":step[3]==="observed"?"is-observed":"is-active";else if(index<stepIndex||stepIndex===fixture.steps.length)tone=source?.[3]==="fail"?"is-fail":source?.[3]==="observed"?"is-observed":source?.[1]==="NOT TESTED"||source?.[1]==="NOT DETECTED"?"is-not-tested":"is-pass";node.classList.add(tone);});}
function renderFixture(fixture,reveal=false){steps.innerHTML=fixture.steps.map((s,i)=>`<li class="demo-step" data-tone="${reveal?s[3]:"unknown"}" data-index="${i}"><span class="step-number">0${i+1}</span><span class="step-name">${s[0]}</span><strong>${reveal?s[1]:"NOT RUN"}</strong><em>${reveal?clean(s[2]):"Awaiting test"}</em></li>`).join("");details.innerHTML=fixture.details.map(([k,v])=>`<div><dt>${clean(k)}</dt><dd>${clean(v)}</dd></div>`).join("");result.dataset.tone=reveal?fixture.tone:"unknown";result.querySelector("strong").textContent=reveal?clean(fixture.overall):"LINKLENS READY";summary.textContent=reveal&&fixture.id==="healthy"?"8 checks · 1.8 sec":"Ready to run a simulated first-pass diagnostic.";switchValue.textContent=reveal&&detailValue(fixture,"Switch")?`Switch  ${clean(detailValue(fixture,"Switch"))}`:"";portValue.textContent=reveal&&detailValue(fixture,"Port")?`Port  ${clean(detailValue(fixture,"Port"))}`:"";vlanValue.textContent=reveal&&detailValue(fixture,"VLAN")?`VLAN  ${clean(detailValue(fixture,"VLAN"))}`:"";observationStatus.textContent=reveal?(fixture.id==="neighbor"?"No advertisement observed":"LLDP advertisement observed"):"Awaiting first pass";observationDetail.textContent=reveal?fixture.explanation:"Neighbor information appears only when advertised.";discoveryFact.textContent=reveal?(fixture.id==="neighbor"?"Not detected":"Observed") : "Not run";scenarioLabel.textContent=reveal?"Run complete":"Ready to run";explanation.textContent=reveal?fixture.explanation:"Ethernet adapter detected. Ready to run a simulated first-pass diagnostic.";next.innerHTML=reveal?fixture.next.map(x=>`<li>${clean(x)}</li>`).join(""):"<li>Run the test to reveal bounded next actions.</li>";resetPath();if(reveal){pathNodes.forEach((_,i)=>setPath(fixture.steps.length,fixture.steps.at(-1),fixture));}}
function start(){timers.forEach(clearTimeout);timers=[];const fixture=scenarioFixtures[select.value];run.disabled=true;run.hidden=false;select.disabled=true;runAgain.hidden=true;report.hidden=true;scenarioExplore.hidden=true;result.dataset.tone="unknown";result.querySelector("strong").textContent="TEST RUNNING";live.textContent="Simulated sequence in progress.";fixture.steps.forEach((step,i)=>timers.push(setTimeout(()=>{document.querySelectorAll(".demo-step").forEach(item=>item.classList.remove("is-active"));const row=steps.children[i];row.classList.add("is-active","is-revealed");row.dataset.tone=step[3];row.querySelector("strong").textContent=step[1];row.querySelector("em").textContent=clean(step[2]);setPath(pathIndex(step[0]),step,fixture);live.textContent=`${step[0]}: ${step[1]}`;if(i===fixture.steps.length-1){renderFixture(fixture,true);run.disabled=false;run.hidden=true;select.disabled=false;runAgain.hidden=false;report.hidden=false;scenarioExplore.hidden=false;live.textContent="Simulated test complete.";}},360+i*300)));}
select.addEventListener("change",()=>{renderFixture(scenarioFixtures[select.value]);runAgain.hidden=true;report.hidden=true;scenarioExplore.hidden=true;});run.addEventListener("click",start);runAgain.addEventListener("click",()=>{renderFixture(scenarioFixtures[select.value]);start();});report.addEventListener("click",()=>document.querySelector("#report").scrollIntoView({behavior:"smooth"}));renderFixture(scenarioFixtures.healthy);
